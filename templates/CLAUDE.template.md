# CLAUDE.md

Read **AGENTS.md** first — it is the source of truth (workflow, rules, the gate, DoD). This file only adds Claude Code notes.

- Work in your worktree on `agent/claude/<task>`; **never commit to `main`** (protected — PR + green CI).
- Run the gate before finishing. A clean merge is not proof; the gate is.
- Smallest correct change; small diffs. No new dependencies without asking.
- When unsure, stop and ask.
